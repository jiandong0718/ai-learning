interface:
  display_name: "审查代码变更"
  short_description: "以代码审查视角检查变更，优先输出 bug、风险与测试缺口"
  default_prompt: "使用 $codebase-review 审查当前变更，优先找出 bug、回归风险、遗漏测试和不合理假设，并按 findings-first 方式汇报。"

policy:
  allow_implicit_invocation: true
